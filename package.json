{
  "name": "showmate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --config wrangler.local.jsonc",
    "build": "vite build",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply showmate-db --local",
    "db:migrate:individual": "wrangler d1 migrations apply showmate-db --remote --config wrangler.individual.jsonc",
    "db:migrate:public": "wrangler d1 migrations apply showmate-db --remote --config wrangler.public.jsonc",
    "deploy": "npm run build && wrangler deploy",
    "deploy:individual": "npm run build && wrangler deploy --config wrangler.individual.jsonc",
    "deploy:public": "npm run build && wrangler deploy --config wrangler.public.jsonc"
  },
  "dependencies": {
    "@vitejs/plugin-react": "4.7.0",
    "partyserver": "0.5.8",
    "partysocket": "1.3.0",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260630.1",
    "@types/react": "19.1.9",
    "@types/react-dom": "19.1.7",
    "typescript": "5.8.3",
    "vite": "7.3.6",
    "vitest": "3.2.7",
    "wrangler": "4.106.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
